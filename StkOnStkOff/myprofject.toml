[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[tool.isort]
profile = "black"
line_length = 79
honor_noqa = true
src_paths = ["stkonstkoff", "tests"]

[tool.black]
line-length = 79
skip_string_normalization = true